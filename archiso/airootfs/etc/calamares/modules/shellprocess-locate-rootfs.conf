---
# Resolve airootfs.sfs before unpackfs. Required when archiso copytoram=auto
# has already unmounted /run/archiso/bootmnt on bare-metal USB boots.
dontChroot: true
timeout: 60
script:
  - "/usr/share/synapseos/locate-rootfs.sh"
